import 'package:flutter/material.dart';
class CreateAdventureScreen extends StatelessWidget {
  const CreateAdventureScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Create Adventure')));
}
